apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: postgres-storage-class
provisioner: kubernetes.io/gce-pd
parameters:
  type: pd-standard  # Use pd-standard if you want HDD
  fstype: ext4
reclaimPolicy: Retain
allowVolumeExpansion: true
